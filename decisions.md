# Decisions — 结构演进决策记录（ADR 风格）

> 关键架构/结构决策的事实来源。版本：1.0 | 2026-08-24（由第五轮审查 P1-1 补齐，从 CHANGELOG 版本历史提取）

## 记录方式

每个决策：**背景 → 决策 → 理由 → 影响**。追加式记录，历史决策不修改（只允许以"修订"追加）。

---

## D-001：schema 版本化副本策略（v1.0 起）

- **背景**：schema 快速演进，旧数据需保持可校验。
- **决策**：根 schema 指向最新版 + 每版 `v<N>/` 不可变副本 + 迁移脚本 + 验证器版本路由。
- **理由**：向后兼容（新字段全部 optional）；历史版本可复现校验。
- **影响**：升级成本 = 4 处同步（根/副本/迁移/前端数据），已机制化为 CHANGELOG 发布清单 + `test_example_data_sync.py`。

## D-002：通用能力复用 OSS，领域逻辑自建（2026-07-09，sun 拍板）

- **背景**：用户明确"GitHub 成熟方案直接复用，不自己实现"。
- **决策**：SheetJS/Tabulator/html2pdf.js 本地化复用；GB/T 50291 估价逻辑无 OSS 可复用 → 自建纯函数引擎（engine.js，Node 双模可单测）。
- **理由**：通用能力省时可靠；领域能力无现成方案且必须精确可控。
- **影响**：vendor 本地化保离线；领域引擎全部可单测。

## D-003：决策链模型（v1.4，2026-08-18）

- **背景**：AI 与估价师在多轮迭代中，被驳回的结论需要保留审计轨迹，且新结论需可追溯到旧结论。
- **决策**：驳回不删除——旧 DP 保留（status=rejected）为不可变审计记录；AI 创建新 DP 用 `supersedes` 指向前驱 + `attempt` 递增，形成决策链；C1-C6 业务校验（存在性/不自引用/前驱必须 rejected/1:1 后继/无环/attempt 一致）。
- **理由**：审计完整性 > 数据整洁；链结构可由校验器强制，弥补 JSON Schema 数组约束不足。
- **影响**：schema v1.4；前端 dp-console 决策链可视化；`test_e2e_orchestrator.js` 闭环锁定。

## D-004：决策点条件约束由描述文本升级为 schema 强制（v1.3.1）

- **背景**：v1.3 的 P0-1~P0-9 约束只写在 description 里，验证器不执行（对抗审查发现）。
- **决策**：`allOf` 内 8 组 `if/then/else` 强制状态一致性、trigger 枚举、riskLevel=risks 最高级等；数组唯一性用业务校验补。
- **理由**：约束必须可执行，不能靠阅读者自觉。
- **影响**：违反即校验失败；单测锁定（v1.3 套件 44→60 用例）。

## D-005：P0-7 riskLevel 必须 = risks 最高级（v1.3.1，联调强化）

- **背景**：AI 重写 successor 后易漏同步 riskLevel（联调发现）。
- **决策**：schema `if/then/else` + `contains` 强制；e2e 测试断言锁定；skill 编排规则明确要求。
- **理由**：风险等级是决策优先级的核心信号，错报会误导估价师。
- **影响**：AI 生成决策包时必须显式计算 risks 最高级。

## D-006：sourceGrade 信源等级结构化（v1.5，2026-08-19）

- **背景**：证据信源质量此前只存在于自由文本 source，无法结构化校验 T0/T1/T2 分级。
- **决策**：`evidenceItem.sourceGrade` enum（T0/T1/T2）可选字段；迁移**不自动推断**（保守，避免误判）；前端徽章展示（T0 绿/T1 蓝/T2 灰）。
- **理由**：结构化后才能做信源质量统计与审查；不推断避免把猜测当事实。
- **影响**：v1.5；22/22 示例 evidence 填充；`test_schema_v15.py` 19 用例。

## D-007：R1 git 历史 PII — 方案 A 声明（2026-08-19，sun 拍板）

- **背景**：早期 commit（742c200 及之前）含虚构人名/证件/地址，git 历史公开可读。
- **决策**：方案 A——README 数据声明覆盖 git 历史；不重写历史（B）、不转私有（C）。
- **理由**：重写历史风险高（公共仓库 force-push）；数据确为虚构，声明已降低误导风险。
- **影响**：已缓解未消除；若后续倾向 B/C 随时切换（HANDOFF-08-19 §2.5）。

## D-008：templates xlsx 入库 + 元数据抖动根治（2026-08-24，第五轮审查）

- **背景**：4 个测算模板 xlsx 入库后，每次全量测试 conftest 重新生成，openpyxl 写入 `dcterms:created/modified` 当前时间戳 → 工作区持续出现二进制 diff（第四次复现）。
- **决策**：方案 ① 元数据固定化——4 个 gen 脚本保存前固定 `wb.properties.created/modified` 为常量；模板保留在版本控制内。
- **理由**：模板对使用者是交付物，入库优于 gitignore；固定元数据使生成结果可复现（deterministic build）。
- **影响**：全量测试后工作区保持干净；HANDOFF §2.6 遗留关闭。

---

## D-009：v9.0 宪章补充"恢复"触发说明（2026-08-24，第五轮审查 P2-4）

- **背景**：第五轮审查以 v9.0 八步流程实战演练时发现——"恢复"步骤无触发条件定义，验证失败/文件损坏/沙箱异常时缺明确的回退路径。
- **决策**：v9.0 新增「恢复协议」章节（置于验证协议与沟通协议之间）：触发条件表（验证失败/文件破坏/沙箱异常/执行偏离）+ 恢复动作（checkout → reset --hard → 重新验证，由轻到重）+ 红线（恢复优先于硬撑；破坏性操作先获批准；根因记入 decisions.md）。
- **理由**：恢复是八步流程的显式步骤，缺触发定义等于未实现；明确回退路径可防止"修补硬撑"导致的状态漂移。
- **影响**：CLAUDE.md v9.0 +51 行；未来所有会话在验证失败时按此协议回退到最近可验证状态。

---

## D-010：C6 浮点语义全镜像 + C4 ghost key 归 Python（2026-08-30，Round 4 / 对抗式审查整改）

- **背景**：对抗式审查（`outputs/对抗式审查-双端校验迭代-20260830.md`）发现 P0-1——Round 2 把 Python C6 入口收窄为 `isinstance(int)`，浮点 attempt（2.0/2.5）双端判定漂移（回归，非原有缺陷）；且 ghost 分叉（双 DP 指向同一不存在 id）JS 多报 C4（P1-2 实证）。审查建议 `is_integer()` 门。
- **决策**：① 不采纳 `is_integer()` 门（会把 2.5 排除出检查、与审查自己的验证预期"S2: 2.5 双端均报 C6"矛盾），改为与 JS `typeof number` **完全镜像**——全部实数（int/float，bool 除外）进入 C6，整数值浮点与整数同权，非整数浮点按数值比较；② ghost key 的 C4 归属按 Python 权威语义：C1 已报存在性，C4 跳过 ghost key（改 JS 而非 Python）。
- **理由**：双端语义对齐的裁决标准是"审查报告的验证预期"而非"修复建议的字面公式"——建议与预期冲突时以预期为准；ghost key 无真实前驱，分叉防护（C4 本义）对不存在的 id 无意义，避免同一问题双报。
- **影响**：回归 315 passed / 32 pass / 差分 18-kind N=1000 100%（912=912）；16-kind 基线 885=885 零扰动。护栏扩展：生成器 16→18 kind + 固化对抗形状测试（`tests/test_diff_chain_consistency.py::test_frozen_adversarial_shapes`）。证据机制：92 例重建档案 `rounds/1/EVIDENCE-RECONSTRUCTED.md` + `rounds/README.md` 存档命名规则（diff_result_roundN.json，禁止原地覆盖）。审查裁决：4 P2 已批量处理；tests/ 反向依赖 rounds/1/ 未解耦（下轮）；未检查边界（buildSuccessorShell/resolveChain 等价性）留作新假设池。
- **修订（2026-08-30 同日）**：tests/ 反向依赖已解耦——生成器/分类器与 Node 执行器迁至 `tests/diff_chain_generator.py` + `tests/chain_runner.js`（唯一事实源；测试 KINDS 由 DIFF_KINDS 派生、去重保序，固化语料序列不变，同时杜绝"清单漏项"型失明），`rounds/1/diff_check_chain.py` 改 CLI 薄壳、`rounds/1/chain_runner.js` 冻结为存档；解耦后 N=1000 复跑 100%（912=912）逐位一致。

---

## 待决策（Open）

| # | 议题 | 选项 | 状态 |
|---|---|---|---|
| O-1 | redLineChecks 语义弱（R2） | 收紧为规则枚举 vs 保持自由文本 | 有意放宽，保留（schema 保证结构，红线判定靠线下专业核验） |
| O-2 | sourceGrade 迁移自动推断 | 保守不推断 vs 从 source 文本 `(Tx)` 解析补全 | 当前保守；若数据量大再议 |
| O-3 | R1 方案 B/C | 重写历史 vs 转私有 | 仅在 sun 改变主意时执行 |
