# WorkBuddy internal metadata
.workbuddy/

# J-space local state (skill controller workspace)
.jspace/

# Temp / extraction files
_extract_规范.txt
_test_outside_workbuddy.md

# Test versions (keep only final report)
outputs/_test_*
outputs/*v[0-9]*.md

# External projects (not part of this package)
storm-deep-research/

# Proprietary reference files (keep as reference only)
房地产估价规范GBT 50291-2015.docx

# Unrelated files
SKILL（房地产经纪人营销获客助手）.md
RH文件夹/

# Python cache
__pycache__/
*.pyc

# Attack test payloads (adversarial review, never commit)
_attack/

# Mutation testing scratch space (rebuilt each run)
tests/_tmp_mutants/

# Local scratch (screenshots/logs/throwaway scripts) — never commit
tmp/

# Working backups of xlsx deliverables (~2MB each, binary, no diff value)
*_bak.xlsx

# 真实委托案件 —— 含真实标的地址，公开仓库不入库。
# 理由：README 声明「仓库所有数据均为虚构」，且 decisions.md D-007 确认 git 历史公开可读；
#       入仓会让真实客户地址随公开历史永久暴露。
# 仓库只保留虚构示例（如 武汉洪山住宅_*）与治理文档。
# 若确实需要留档，先用 scripts/anonymize_template.py + anonymize_rules.yaml 匿名化。
# 具体案名的 glob 放在 .git/info/exclude（本地专属、不入库），避免案名本身进公开历史。
cases/
