# 2026-08-07 任务交接文档

> **日期**：2026-08-07  
> **仓库**：https://github.com/sunhe-8922/real-estate-appraisal-toolchain  
> **规范**：GB/T 50291-2015《房地产估价规范》  
> **前置文档**：`HANDOFF.md`（2026-08-03 总交接）、`README.md`  
> **下游读者**：下一个接手本项目的 AI 助手或开发者（无需对话上下文）

---

## 一、今日已完成

### 1.1 ROUND 精度控制 — 全部 4 个 Excel 模板计算公式

**背景**：openpyxl 生成的公式均缺少 `=ROUND(formula, N)` 包装，可能导致浮点误差（如 24999.9999 → 显示 25000 但后续 SUM 出偏差）。

**修改**：四个 `scripts/gen_*.py` 中所有计算类公式增加 `=ROUND(..., 0)`：

| 脚本 | 修改处数 | 关键公式示例 |
|------|:--:|------|
| `gen_comps_template.py` | 0 | 比较价格/平均值/中位数此前已有 ROUND，无需修改 |
| `gen_income_template.py` | +3 | `=ROUND(SUM(D6:D65),0)` — 收益价值合计 |
|  |  | `=ROUND(IF(C69*C70=0,0,C69/(1+C70)^C3),0)` — 期末转售折现 |
|  |  | `=ROUND(D66+D70,0)` — 收益总价值 |
| `gen_cost_template.py` | +6 | `=ROUND(SUM(C6:C12),0)` — 重置成本合计 |
|  |  | `=ROUND((B5-B6)*MAX(0,MIN(1,B7/B8)),0)` — 直线法折旧 |
|  |  | `=ROUND(B5*(1-B9),0)` — 成新折扣法折旧 |
|  |  | `=ROUND(IF(...),0)` — 折旧合计 |
|  |  | `=ROUND(B4-B5+B6,0)` / `=ROUND(B7*B9,0)` — 成本价值/总价值 |
| `gen_hypo_dev_template.py` | +17 | 管理费/销售费/税费 ROUND |
|  |  | 6 个折现项各自 ROUND |
|  |  | 支出合计 SUM +ROUND |
|  |  | 开发价值总价 & 单价 +ROUND |
|  |  | 现金流 D 列净现金流 +ROUND |
|  |  | Sheet2 NPV SUMPRODUCT +ROUND |

**未修改**（合理保留）：折现系数因子（需6位小数精度）、百分比比率显示（`0.0%` 格式）。

**验证**：openpyxl spot-check 15 个关键单元格，100% 含 ROUND。四个 `.xlsx` 重新生成无报错。

**输出文件**：`outputs/templates/` 下四个 `.xlsx` 已全部更新（2026-08-07 12:32）。

**Git**：commit `72dbab7`（8 files, +35/-35），已推送 master。

---

### 1.2 re-appraisal-expert 测试方案

**背景**：HANDOFF.md §5.2 第 6 项 "激活测试 re-appraisal-expert" —— 专家的 202 行系统提示词已完成设计，但从未在实际对话中验证回复质量。

**产出**：`outputs/re-appraisal-expert_测试方案.md`

5 个测试 Case 覆盖专家全部能力维度：

| Case | 测试焦点 | 红线 |
|------|---------|:--:|
| Case 1 | 住宅抵押估价方法选用（Phase 1-2） | — |
| Case 2 | 商业用房比较法 — 单项修正 +22%、综合修 ~58%、日期距价值时点 26 个月 | ⚠️ 4 红线全触发 |
| Case 3 | 报告合规审查 — 故意缺失"假设和限制条件"章节 | — |
| Case 4 | 抵押净值计算 — 谨慎原则 & 法定优先受偿款区分 | — |
| Case 5 | 委托人要求高估（500→650 万）— 职业道德边界 | ⚠️ 必过 |

每个 Case 含预期行为检查表、权重标注、评分标准（S/A/B/C 四级）。

**执行方法**：在 WorkBuddy 左侧栏「专家」→ 选择「房地产估价专家」→ 逐 Case 输入 → 对照检查表评分。

**HANDOFF.md 状态**：#6 已标记为 🧪 测试方案已出，待执行。

---

### 1.3 HANDOFF.md 维护

| 项号 | 状态变更 |
|:--:|------|
| #2 | ~~实际生成可用的 .xlsx 模板~~ → ✅ 已完成（2026-08-07），备注更新为含 ROUND + 已推送 |
| #6 | → 🧪 测试方案已出（2026-08-07），指向 `outputs/re-appraisal-expert_测试方案.md` |

---

## 二、未完成 / 待办

### 2.1 高优先级（v1.1）

| # | 任务 | 状态 | 关键信息 |
|:--:|------|:--:|------|
| 1 | **修复收益法住宅场景默认策略** | ❌ 未开始 | 当前报酬资本化法低估 50%（146 万 vs 311 万），应触发"持有加转售模式"作为住宅默认。详见 `HANDOFF.md` §4.3 |

### 2.2 中优先级（v1.2）

| # | 任务 | 状态 | 关键信息 |
|:--:|------|:--:|------|
| 5 | **实现所有 5 种报告输出格式** | ❌ 未开始 | 当前仅 Markdown 验证通过。Word/HTML/PDF/Dashboard 在 `appraisal-report` SKILL.md 中已有指令但未实现 |
| 6 | **执行 re-appraisal-expert 测试** | 🧪 方案已出 | `outputs/re-appraisal-expert_测试方案.md`，需切换到专家对话逐 Case 验证 |
| 7 | **增加单元测试** | ❌ 未开始 | 四条测算技能核心计算逻辑需 Python 单测（`node --test` 运行 engine.js 模式亦可）。当前无任何自动化测试 |
| 8 | **批量报告生成** | ❌ 未开始 | 一套模板 + 多套数据 = 批量报告，已设计但零实现 |

### 2.3 低优先级（v2.0）

| # | 任务 | 状态 |
|:--:|------|:--:|
| 9 | 支持地方性补充规定（深圳/上海等） | ❌ |
| 10 | 估价报告对比/差异分析（git diff 风格） | ❌ |
| 11 | 估价项目历史管理（按项目组织+复用） | ❌ |
| 12 | Web 前端集成（对接 gujia开发 的 index.html + engine.js） | ❌ |

---

## 三、需要注意的风险

### 3.1 高风险

#### R1：收益法住宅场景公式错误（已知bug，未修）

- **现象**：报酬资本化法对住宅场景低估约 50%
- **根因**：住宅剩余经济寿命通常 50-70 年，全剩余寿命折现对远期收益极度敏感，报酬率微小偏差导致巨大结果波动
- **规范要求**：4.3.3 条明确"收益期长时宜选持有加转售模式"
- **影响**：任何住宅抵押估价使用收益法时都可能是错误的
- **建议修复**：收益法 SKILL.md 中增加"估价对象类型检测"逻辑 → 住宅自动切换持有加转售模式 → 转售价值用比较法估算

#### R2：无自动化测试，回归风险高

- 4 个 gen_*.py 脚本共 26 处 ROUND 修改，无单测覆盖
- Excel 模板重新生成后依赖人工 openpyxl spot-check，非自动化
- 技能 SKILL.md 中 JSON Schema 集成也未验证
- **建议**：至少为 gen_*.py 脚本加 pytest（验证关键公式字符串包含正确的 ROUND/SUM/IF），为 engine.js 加 node --test

#### R3：re-appraisal-expert 未实战验证

- 202 行提示词质量未知，可能存在的坑：
  - Case 2 的 4 条红线能否全部识别并正确引用条款号
  - Case 5 能否果断拒绝"调整参数达到目标"而非给模糊建议
  - Phase 4（估价结果确定）的三步流程（校核→比较→调整）是否按规范执行
- **建议**：优先执行 Case 2 和 Case 5，这两条最能暴露提示词质量

### 3.2 中风险

#### R4：JSON Schema 集成无端到端验证

- 5 个 SKILL.md 已写入 JSON 输出/输入章节
- 但实际对话中各技能是否可靠输出 JSON 未知
- 降级策略已设计（无 JSON → 自然语言 + `sourceMode: naturalLanguage`），但降级后的报告质量未测试

#### R5：Excel 模板跨表引用正确性

- 成本法 3 Sheets 联动、假设开发法现金流时间线跨 Sheet 引用
- 公式生成后在 Excel 中打开可能因跨表引用路径问题报错（openpyxl 的跨表公式语法与 Excel 引擎不完全一致）
- **建议**：在 Excel 2016+ 中打开验证一次

### 3.3 低风险

#### R6：install.ps1/install.sh 不覆盖已有技能

- 当前设计为直接复制覆盖，不检查已有版本
- 如果用户手动修改过技能但未提交 Git，install 会静默覆盖
- **建议**：后续版本增加备份机制（`--backup` 参数）

---

## 四、后续迭代建议

### 4.1 建议的下一步（按优先级）

```
Step 1: 修复 R1（收益法住宅场景）     ← 阻塞所有住宅抵押估价
Step 2: 执行 R3（re-appraisal-expert 测试 Case 2+5）  ← 验证专家质量
Step 3: 实施 R2（gen_*.py 单测）       ← 杜绝回归
Step 4: 实施 #5（Word/HTML/PDF 报告格式）  ← 补齐输出能力
```

### 4.2 架构层面

- JSON Schema 集成已经为"技能即函数"架构打下基础（`Skill A → JSON → Skill B`）
- 下一步可以考虑引入 `Orchestrator` 概念：一个入口技能接收用户指令 → 按估价目的指派方法组合 → 收集各方法 JSON → 统一调用 appraisal-report
- 这比当前"用户手动逐一调用技能"的模式更自动化，也更像真实估价师的工作流

### 4.3 工程层面

- 当前 gen_*.py 脚本和 engine.js 分别用 Python/Node，构建链路不统一
- 可考虑全部收拢到 Node（纯函数 engine.js + xlsx 生成用 SheetJS），减少跨语言依赖
- 或者反之全部收拢到 Python（openpyxl 更成熟），取决于 `gujia开发` 项目的 index.html 最终形态

---

## 五、关键路径与文件索引

| 需要了解的内容 | 读这个文件 |
|--------------|----------|
| 项目总览 + 交付物清单 + 操作速查 | `README.md` |
| 完整任务状态 + 架构设计 + 踩坑记录 | `HANDOFF.md` |
| 今天做了什么 + 当前风险 | `HANDOFF-2026-08-07.md`（本文件） |
| re-appraisal-expert 测试方案 | `outputs/re-appraisal-expert_测试方案.md` |
| 四个模板最新状态 | `outputs/templates/*.xlsx` |
| 生成脚本 | `scripts/gen_comps_template.py` / `gen_income_template.py` / `gen_cost_template.py` / `gen_hypo_dev_template.py` |
| JSON 数据契约 | `schema/appraisal-result.schema.json` |
| 安装到 WorkBuddy | `install.ps1`（Windows）或 `install.sh`（Unix） |
| 今日开发日志 | `.workbuddy/memory/2026-08-07.md` |
| Git 历史 | `git log --oneline`（最近：72dbab7, 84176d2, caa7c29, 7169a81, 0a42df0, 5d6458f） |

---

*本文件为自包含的交接文档，不依赖当前对话上下文。下一个 AI 助手或开发者可直接据此理解项目状态并继续工作。*
