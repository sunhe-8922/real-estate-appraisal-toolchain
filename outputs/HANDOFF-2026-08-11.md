# 任务交接文档 — 对抗式审查修复（2026-08-11）

> **创建时间**：2026-08-11 18:55
> **继承自**：`outputs/对抗式审查报告_2026-08-11.md` + 本次修复任务
> **状态**：全部 7 项任务已完成，待用户决策 P0 git 历史 PII 处理方案

---

## 一、已完成功能点

### 1.1 P0 安全：README 数据虚构标注

- **文件**：`README.md` 首行
- **内容**：声明所有人物姓名、证件号、电话、地址、机构名称均为虚构测试数据
- **影响**：解决"公开仓库 git 历史 PII"的部分风险——当前版本无真实数据；但历史 commit 仍需另行处理（见 §二）

### 1.2 P1 技能同步：用户级副本更新

- **命令**：`install.sh --skills-only --force`
- **结果**：7 个技能已同步至 `~/.workbuddy/skills/`
- **验证**：`install.sh --check` 显示 7/7 项目级 + 7/7 用户级

### 1.3 P1 Schema 加固（6 处约束 + FormatChecker）

**schema/appraisal-result.schema.json** 修改：

| 位置 | 修改 | 拦截的攻击 |
|------|------|-----------|
| `methods` | `minProperties: 1` + `additionalProperties: false` | 攻击 1（空 methods）、攻击 7（fakeMethod） |
| `result.weightSum` | `type: number` → `const: 1.0` | 攻击 2（weightSum=999） |
| `comps.comparableInstances` | `minItems: 3` | 攻击 8（仅 1 个可比实例） |
| 4 个方法的 `redLineChecks` | 各加 `minItems: 1` | 攻击 4（空红线检查） |
| `project`/`property`/`valuation`/`result` | 各加 `additionalProperties: false` | 攻击 5（拼错字段名） |

**scripts/validate_appraisal_json.py** 修改：

- `validate_full` + `validate_fragment`：启用 `jsonschema.FormatChecker()`，`format: "date"` 生效（拦截攻击 3）
- `validate_degraded`：实现文档承诺的红线检查（至少一个方法 redLineChecks 非空）

### 1.4 P1 对抗测试固化

- **新文件**：`tests/test_adversarial.py`
- **用例**：10 个，覆盖审查报告的 8 个攻击 + 2 个额外扩展
- **结果**：10/10 全绿
- **基线变化**：测试总数 78 → 88

### 1.5 P1 Excel 模板占位符修复

| 脚本 | 修改内容 |
|------|---------|
| `gen_comps_template.py` | B6 `'如: 25000'` → `25000`；B7-B12 `'1.00'` → `1.0` |
| `gen_income_template.py` | B6 `'如: 600'` → `600`；B7 `None` → `0.05`；B9 `'如: 50'` → `50` |
| `gen_cost_template.py` | 公式改为绝对引用 `$F$8`/`C14*$F$9` 等；F8-F12 填充默认值 |
| `gen_hypo_dev_template.py` | B15 填 `25000` |

**重新生成**：4 个 `.xlsx` 模板已更新，打开后无 #VALUE!/#DIV/0!

### 1.6 P1 install.sh bug 修复

- `((found++))` 在 `set -e` 下会退出 → 改为 `found=$((found+1))`
- `--check` 输出混乱 → 改用全局变量 `VERIFY_COUNT`，明细行与汇总行分离显示

### 1.7 P2 HANDOFF 行数修正

- 技能行数更新：comps 376→434、income 387→436、cost 370→424、hypothetical-dev 390→439、appraisal-report 748→838
- example 描述：`"基于测试案例"` → `"独立示例，非硬编码"`

---

## 二、未解决问题

### 2.1 [P0] git 历史 PII 未处理

- `git log -S '张明远'` 命中 3 个 commit（含 v1.0 初始提交 `742c200`）
- 公开仓库下任何人均能执行 `git log -p` 读到原始姓名/电话/地址
- **三选一待决策**：A（已选：README 标注虚构）/ B（`git filter-repo` 重写历史，force push）/ C（仓库转 private）

### 2.2 [P2] HANDOFF §5.4 架构图过时

- 架构图仍画"纯自然语言协作"，与 §4.2"数据孤岛已解决"矛盾
- 建议：下次维护 HANDOFF 时更新架构图

---

## 三、需要注意的风险

| # | 风险 | 影响 | 应对 |
|---|------|------|------|
| R1 | Schema `const: 1.0` + `minItems` 变更 | 外部依赖旧 schema 的调用方会报非法 | 已在 README 标注 schemaVersion 1.0；对外发布时同步 changelog |
| R2 | `validate_degraded` 新增强制检查 | 手动构造的合法降级数据若缺红线检查会被拒 | 已有 degradation fixture 通过；用户手动构造时需确保至少一个方法有 redLineChecks |
| R3 | 成本法公式路径变更 | `C7*管理费率` → `C7*$F$8` | 模板已更新；如有旧版模板备份需注明"已废弃" |

---

## 四、后续迭代建议

### 短期（1-2 周）

1. **完成 P0 git 历史 PII 决策**：执行 B 或 C
2. **扩展对抗测试**：增加边界值测试（浮点精度）、类型注入测试（number 字段传 string/null）
3. **更新 HANDOFF §5.4 架构图**：对齐 §4.2 "数据孤岛已解决"状态

### 中期（1-2 月）

4. **Schema 版本化**：引入 `schema/v1.1` 迁移机制 + `CHANGELOG.md`
5. **模板用户试用**：收集实际使用反馈，补充"使用说明"sheet
6. **端到端回归测试**：自然语言输入 → schema 验证 → 报告生成 全链路测试

### 长期（季度）

7. **对抗式审查常态化**：每轮迭代固定运行对抗测试，攻击清单持续扩充
8. **公开仓库安全审计**：定期 `git log -S <PII关键词>` 扫描，建立 PII 清理流程

---

## 五、交付物清单

| 文件 | 说明 |
|------|------|
| `README.md` | 新增数据虚构声明 |
| `schema/appraisal-result.schema.json` | 6 处约束加固 |
| `scripts/validate_appraisal_json.py` | FormatChecker + validate_degraded 实现 |
| `tests/test_adversarial.py` | 10 个对抗测试（新增） |
| `scripts/gen_*.py`（4 个） | 占位符改真实数值 |
| `outputs/templates/*.xlsx`（4 个） | 重新生成 |
| `install.sh` | 修复算术语法 + --check 输出 |
| `HANDOFF.md` | 行数声明 + example 描述修正 |
| `outputs/HANDOFF-2026-08-11.md` | 本交接文档 |

---

## 六、验证命令

```bash
# 全量测试（应 88/88 全绿）
python -m pytest tests/ -v

# 仅对抗测试
python -m pytest tests/test_adversarial.py -v

# 检查安装状态
./install.sh --check

# 验证 schema 合法性
python -c "import json, jsonschema; jsonschema.Draft202012Validator.check_schema(json.load(open('schema/appraisal-result.schema.json'))); print('OK')"
```

---

*本交接文档继承自 `outputs/对抗式审查报告_2026-08-11.md`，作为下一个相关任务的输入依据。*
