# 任务交接文档 — 2026-08-14 ~ 2026-08-15

> **生成时间**：2026-08-15 14:20（终版，覆盖本轮全部工作）
> **继承自**：`outputs/HANDOFF-2026-08-14.md`（v1.2 schema + Excel 计算链初版）
> **状态**：全部任务完成，**186/186 测试全绿，已推送 GitHub master**
> **前置文档**：`outputs/HANDOFF-2026-08-14.md`、`outputs/对抗式审查报告_2026-08-15.md`

---

## 一、本次完成的功能

### 1.1 Schema v1.2 — 修正系数多子项支持

| 新增 | 说明 |
|------|------|
| `adjustments.locationDetails[]` | 区位修正子项（12 项，对应 Excel 行7-18），100 基准指数 |
| `adjustments.physicalDetails[]` | 实物修正子项（8 项，对应 Excel 行24-31），100 基准指数 |
| `adjustments.interestDetails[]` | 权益修正子项（5 项，对应 Excel 行19-23），100 基准指数 |
| `$defs.factorDetail` | `{required:[name,factor], additionalProperties:false}` 结构约束 |
| `$defs.calculationNode` | calculationChain 节点定义 |
| 根 schema `schemaVersion` pattern | `"^1\.2$"` |

**关键设计决策**：
- 子项用 **100 基准指数**（100=相同，>100=优，<100=劣），顶层系数保留小数倍率
- **合并口径修正为偏差加总法**：`100/(100+Σ(f-100))`，与 Excel 实际算法一致（非连乘法）。此修正由 v1.1→v1.2 测试锁定，不再依赖口头约定
- 顶层系数仍为合并值，与子项可互推（实测示例数据自洽 F1-F8 全一致）

### 1.2 Excel 公式 → JSON 计算链

| 脚本 | 职责 |
|------|------|
| `scripts/extract_calculation_chain.py` | 从 Excel 提取公式 → JSON `calculationChain` |
| `scripts/rebuild_excel_formula.py` | Schema → Excel 公式重建，双模式 |

**calculationChain 结构**（已固化在 `outputs/calculation_chain.json`）：
```json
{
  "version": "1.2",
  "nodes": [
    {"id": "comps.adjustedUnitPrice.instance1", "formula": "...", "refs": {...}, ...},
    // ... 共 8 节点
  ]
}
```

**rebuild_excel_formula.py 双模式**：
- `--mode values`：数值求值验证，对比 Excel 原公式计算结果（7/8 PASS，`income.value` SKIP 是数据口径问题）
- `--mode cells`：单元格引用重建，与 Excel 原公式归一化后 **8/8 逐字一致**

**公式模板语法**：
- `{{refKey}}` — 引用 refs 中的数据值
- `SUM({{refKey}})` — Details 数组取 factor 并求和
- 数值常量直接内嵌（如权重 0.5/0.3/0.2）
- 局部中间量保留原 Excel 单元格引用（如 G23）

**PRECONDITIONS**（计算链生效的前提）：
- Excel 模板必须位于 `outputs/房地产评估明细表-计算模板.xlsx`
- 提取脚本依赖 `openpyxl` + 正则映射表；如模板 sheet 结构变化，需同步更新 `extract_calculation_chain.py` 中的布局常量
- 公式节点手动维护（AI 生成 calculationChain 后需人工核对公式正确性）

### 1.3 安全修复 — 对抗式审查第二轮

详见 `outputs/对抗式审查报告_2026-08-15.md`。

| 发现 | 严重度 | 状态 |
|------|:--:|:--:|
| `eval()` 沙箱可逃逸执行任意命令 | **P0** | ✅ 已修复 + 13 回归测试 |
| schema 放行恶意 formula（随 #1 修复） | P1 | ✅ 已封堵 |
| factor 可为负数/0（合并公式无意义） | P1 | ✅ 已修（exclusiveMinimum: 0） |
| migrate 在 result=null 时丢失 calculationMode | P1 | ✅ 已修复 + 回归测试 |
| redLineChecks.rule 任意文本/passed 全 false 仍过 | P2 | ⚠️ 设计权衡，未改 |
| income.value SKIP（directCapitalization 无 growthRate） | P2 | ⚠️ 已知数据口径，非缺陷 |

**核心教训**：第一轮 PII 治理已纠正"声明先行、验证滞后"，但 v1.2 引入 eval 时又犯同类错误——用注释声明"非外部输入"而非代码约束。**凡对输入性质的安全声明，必须用测试证明，不能只写在注释里。** 本次已将逃逸攻击固化为 `test_adversarial_v12.py`。

### 1.4 safe_eval 白名单扩展

在 P0 修复基础上扩展，支持更多 Excel 函数：

| 新增 | 说明 |
|------|------|
| `MAX` / `MIN` | 多参数取最大/最小 |
| `ABS` | 绝对值 |
| `AVERAGE` | 算术平均 |
| `POWER(x, n)` | 幂运算（`^` 的函数写法） |
| `INT(x)` | 截断取整 |
| `SQRT(x)` | 平方根 |
| `IF(cond, true, false)` | **惰性求值**——只 walk 匹配分支，未取分支不执行 |
| `AND(...)` / `OR(...)` | 逻辑与/或（Excel 函数式） |
| 比较运算符 | `= <> < <= > >=`（AST Compare 单次比较，拒绝链式） |
| 预处理 | `<>` → `!=`（Excel 语法转 Python） |

**安全不变量**（未放宽）：
- `BoolOp`（Python `and`/`or` 运算符）仍被拒绝——用 `AND()`/`OR()` 函数代替
- 属性访问/下标/推导式/lambda/名称引用仍全部拒绝
- 8 条逃逸 payload 仍全部拦截

---

## 二、未解决的问题

### 2.1 [P0] 遗留 R1 — git 历史含第一轮前 PII

commit `742c200` 及之前提交包含真实项目数据（含敏感信息）。公开仓库任何人均可通过 `git log -p` 读取历史。

**处置选项**（第一轮已评估）：
- **A**：确认虚构数据声明有效（README 已声明），关闭 R1
- **B**：重写历史（`git filter-branch` 或 BFG），代价：所有 clone 的仓库需要 force-pull，影响协作者
- **C**：转私有仓库，阻止公开访问历史

**建议**：如仓库仍保持公开，倾向 B；如计划转私有，倾向 C；如已确认数据均虚构无实际风险，可 A 关闭。

### 2.2 [P1] redLineChecks 语义弱（未改，设计权衡）

实测 `rule` 可填任意文本、`passed` 可全 false 仍通过 schema。这是有意放宽——红线判定依赖线下专业核验，schema 只保证"有检查记录"这一结构，不代行业务判断。validate_degraded 已强制"至少一个方法红线非空"。

**若需收紧**：
- `rule` 改为 enum（固定 7 条规范红线规则名）
- `passed=false` 时必填 `detail`（含问题描述 + 修正建议）

### 2.3 [P2] income.value SKIP 在 values 模式

示例数据采用 directCapitalization 模式，无 `growthRate` 字段，导致该节点无法求值。该节点公式适用于 fullRemainingLife/holdAndResale 模式。示例数据升级 v1.2 时选择保留 directCapitalization，未引入 growthRate。

**建议后续**：若需完全覆盖，可在示例中增加 fullRemainingLife 模式案例，或把 income.value SKIP 视为已知数据缺口文档化（HANDOFF 已有说明）。

---

## 三、需要注意的风险

| # | 风险 | 影响 | 应对 |
|---|------|------|------|
| R1 | git 历史含 PII（见 §2.1） | 公开仓库可 git log -p 读取 | 待 sun 决策 A/B/C |
| R2 | redLineChecks 语义弱（见 §2.2） | AI 可填假红线记录 | 决策是否收紧为 enum + detail 必填 |
| R3 | calculationChain 节点公式需手动维护 | 模板 sheet 结构变化时公式失效 | 变更模板时同步更新 extract 脚本和 calculation_chain.json |
| R4 | 提取脚本硬编码 Excel 列位置 | 实例数量/列布局变化时提取错乱 | 变更需重跑提取 + 人工核对 |
| R5 | rebuild_excel_formula 依赖 outputs/ 路径 | 路径变化时脚本找不到模板 | 可通过 `--excel` 参数指定路径，或使用相对路径环境变量 |
| R6 | safe_eval 白名单仍需扩展 | 未来若 formula 用 IF/MAX 等新函数需扩白名单 | 扩展时必须同步补充恶意用例测试（防回归） |

---

## 四、后续迭代建议

### 短期（1-2 周）

1. **处理 R1（git 历史 PII）**：这是最高优先级，选 A/B/C 之一并执行
2. **补全示例数据**：在 `example-武汉洪山住宅.json` 中增加 fullRemainingLife 模式案例，覆盖 income.value 求值路径
3. **HANDOFF 同步**：更新 `outputs/HANDOFF-2026-08-14.md` 把 R1/R2/R3/R4/R5/R6 纳入风险表

### 中期（2-4 周）

4. **v1.3 规划中的剩余字段**（HANDOFF §四中期规划已标注，本次未实施）：
   - `property.internalArea`（套内建筑面积，Excel 独有）
   - `property.landUseRightExpiryDate`（土地使用权到期日）
   - `income.netOperatingIncome.operatingExpenseItems[]`（运营费用明细）
   - `result.originalPurchasePrice`（原始购置价款）
5. **calculationChain 双向同步**：Schema → Excel 公式重建已打通，但 Excel → Schema 数据回填尚未自动化（目前靠手工核对）
6. **对抗式审查第三轮**：针对 v1.3 新增字段构造畸形输入，重点覆盖 operatingExpenseItems[] 的数值范围约束

### 长期

7. **Excel 模板抽象化**：当前提取脚本硬编码列位置，后续可考虑用 SheetJS 解析 sheet 结构自动推断
8. **测试覆盖继续扩大**：当前 186 用例覆盖 schema/迁移/对抗/模板，可进一步加入端到端场景（Excel → JSON → 报告生成全链路）

---

## 五、验证命令

```bash
# 全量测试（应 186/186 全绿）
python -m pytest tests/ -q

# 仅对抗测试（第二轮新增 13 + 原有 10 = 23）
python -m pytest tests/test_adversarial_v12.py tests/test_adversarial.py -v

# 仅 schema 测试
python -m pytest tests/test_schema_v12.py tests/test_json_schema.py -v

# 重建验证（values 模式应 7/8 PASS，cells 模式 8/8 一致）
python scripts/rebuild_excel_formula.py --mode values
python scripts/rebuild_excel_formula.py --mode cells

# 验证 root == v1.2 schema 一致
python -c "
import json
r = json.load(open('schema/appraisal-result.schema.json', encoding='utf-8'))
s = json.load(open('schema/v1.2/appraisal-result.schema.json', encoding='utf-8'))
print('一致:', {k: v for k, v in r.items() if k != '\$id'} == {k: v for k, v in s.items() if k != '\$id'})
"

# 检查 git 未提交变更
git status --short
```

---

## 六、架构现状（已更新）

### Schema 版本体系

```
schema/
├── appraisal-result.schema.json        ← 根目录（v1.2，pattern: "^1\.2$"）
├── example-武汉洪山住宅.json            ← 示例数据（schemaVersion: "1.2"）
├── v1.0/
│   └── appraisal-result.schema.json    ← 历史快照（const: "1.0"，不可变）
├── v1.1/
│   └── appraisal-result.schema.json    ← 版本化副本（与根目录一致）
└── v1.2/
    └── appraisal-result.schema.json    ← 版本化副本（与根目录一致）
```

### 核心脚本链路

```
Excel 计算模板
    │
    ├── extract_calculation_chain.py（提取公式 → calculationChain JSON）
    │       │
    │       └── outputs/calculation_chain.json
    │               │
    │               ├── rebuild_excel_formula.py（cells/values 模式）
    │               │       ├── values 模式：求值验证（7/8 PASS）
    │               │       └── cells 模式：公式重建（8/8 逐字一致）
    │               │
    │               └── schema（calculationChain 字段约束）
    │
    └── JSON (schemaVersion: "1.2")
            │
            ├── validate_appraisal_json.py 验证
            ├── migrate_schema.py 版本迁移（如需）
            └── appraisal-report 技能生成报告
```

---

## 七、交付物清单

### 本次新增文件

| 文件 | 说明 |
|------|------|
| `scripts/extract_calculation_chain.py` | Excel 公式提取 → JSON calculationChain |
| `scripts/rebuild_excel_formula.py` | Schema → Excel 公式重建（双模式） |
| `tests/test_schema_v12.py` | v1.2 schema 专项测试（37 用例） |
| `tests/test_adversarial_v12.py` | 对抗式审查第二轮测试（13 用例，固化 eval 逃逸攻击） |
| `outputs/calculation_chain.json` | 从示例 Excel 提取的 8 节点计算链 |
| `outputs/对抗式审查报告_2026-08-15.md` | 第二轮对抗式审查完整报告 |
| `outputs/HANDOFF-2026-08-14.md` | 首日交接文档（本次已更新） |

### 本次修改文件

| 文件 | 变更 |
|------|------|
| `schema/appraisal-result.schema.json` | schemaVersion pattern → `^1\.2$`，新增 factorDetail/calculationNode，factor exclusiveMinimum: 0 |
| `schema/v1.2/appraisal-result.schema.json` | 同步根目录变更 |
| `schema/example-武汉洪山住宅.json` | 升级到 v1.2：details 三数组 + 精确顶层系数 + 全链自洽重算 + calculationChain 示例 |
| `scripts/migrate_schema.py` | result=null 时写回推断值 |
| `scripts/rebuild_excel_formula.py` | eval → safe_eval（AST 白名单），扩展 IF/MAX/MIN/ABS/AVERAGE/POWER/INT/SQRT/AND/OR + 比较运算 |
| `tests/test_schema_migration.py` | fixture 剥离 v1.2 字段（v1.1 schema 会拒绝 calculationChain） |
| `tests/fixtures/degradation_natural_language.json` | schemaVersion → 1.2 |
| `CHANGELOG.md` | 新增 [1.2] 条目 |

---

## 八、Git 状态

```
最新提交: 7238595 (origin/master)
  feat: safe_eval 白名单扩展 IF/MAX/MIN/ABS/AVERAGE/POWER/INT/SQRT/AND/OR + 比较运算

本轮提交链:
  e3daab9 ← d62b1a4 ← 659e9e3 ← 1a755a8 ← 7238595
  （v1.1 schema）  （v1.2 schema）  （安全修复）  （白名单扩展）
```

**工作区**：干净，无未提交变更。

---

*本交接文档独立于对话上下文，下一位接手者可仅凭本文档 + 关联文件（对抗式审查报告、CHANGELOG）恢复项目状态。*
