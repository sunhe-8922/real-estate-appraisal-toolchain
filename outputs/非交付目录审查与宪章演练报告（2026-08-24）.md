# 非交付目录审查与宪章演练报告（2026-08-24）

> **性质**：第五轮对抗式审查 §五 覆盖声明的补做项
> **方法**：以 v9.0 宪章八步流程（目标→上下文→证据→决策→执行→验证→恢复→交付）作为本次审查本身的执行框架——同时检验宪章在真实任务中的可执行性
> **结论先行**：**0 安全风险**；2 类残留待清理（需 sun 批准）；**v9.0 宪章八步流程在真实任务中可执行**，"安全停止"机制正确触发

---

## 一、宪章演练结果（八步流程逐项）

| 步骤 | 本任务中的落点 | 执行情况 |
|------|--------------|---------|
| 1 目标 | "审查 .workbuddy/ 与 storm-deep-research/ 内部变化" | ✅ 明确，一句话可述 |
| 2 上下文 | .gitignore 规则（两目录均被忽略）+ 审查报告 §五 + git 跟踪边界 | ✅ 最小充分，未读无关文件 |
| 3 证据 | ls/du/md5/diff/grep 取证（见 §二） | ✅ 多维度交叉，含安全专项 |
| 4 决策 | 0 安全风险 + 2 类残留（见 §三） | ✅ 结论先行，分类明确 |
| 5 执行 | 清理动作**停在审批边界** | ⏸️ 按宪章"破坏性操作需人工批准"暂停 |
| 6 验证 | （待 sun 批准清理后执行） | — |
| 7 恢复 | 残留文件均为 untracked，删除不可逆但无内容损失（副本在 git 有版本） | ✅ 预案已明 |
| 8 交付 | 本报告 | ✅ |

**宪章可执行性判定**：✅ **可执行**。关键验证点是第 5 步——宪章的停止条件（"下一步是未经授权的破坏性操作"）在真实场景**正确触发**，AI 没有擅自删除文件，而是交付决策包等待批准。"安全停止优于失控执行"从文字变成了真实行为。

**演练中发现的一个宪章薄弱点（供 sun 参考）**：八步流程的"恢复"步骤没有明确定义触发条件（什么情况算需要恢复、恢复到哪个状态）。本任务因操作尚未执行而留空；建议在 v9.0 中补充"恢复"的触发说明（例如：验证失败 / 文件被破坏 / 沙箱异常时，恢复到最近一次 commit 状态）。

---

## 二、证据链

### 2.1 `.workbuddy/`（项目内部元数据，git 忽略）

| 子目录 | 内容 | 判定 |
|--------|------|------|
| `memory/` | 15 个日志（07-09 ~ 08-24）+ MEMORY.md | ✅ 正常，跨会话一致 |
| `skills/` | 8 技能（含 orchestrator） | ✅ 与 `skills/` 根目录 `diff -rq` 零差异 |
| `schema/` | v1.5 副本 + 示例 | ✅ 与根目录 md5 完全一致（appraisal-result.schema.json / example-武汉洪山住宅.json） |
| `outputs/` | 8 个 `.md`（08-03 旧版报告 v1~v5 + 3 个 `_test_*` 测试文件） | ⚠️ **残留**：08-03 之前的旧产物，正式版已在 `outputs/` 根（v5），此副本冗余且违反"交付件统一放 outputs/"约定 |
| `tmp/` | 12 个文件（08-23 的 webbridge 浏览器自动化测试残留，`wb-eval*.json` / `wb-s*.json` 等） | ⚠️ **残留**：一次性测试产物，无保留价值 |

### 2.2 `storm-deep-research/`（外部独立项目，git 忽略）

| 检查项 | 结果 | 判定 |
|--------|------|------|
| 仓库性质 | 独立嵌套 git 仓库（`bbf2583`，单提交） | ✅ |
| 父仓库跟踪 | `git ls-files storm-deep-research/` = 0；`git check-ignore` 确认忽略 | ✅ 无泄漏入主仓库 |
| 体积 | 67M，其中 `.venv` 66M、`.git` 256K、`.pytest_cache` 15K | ⚠️ 磁盘占用（非泄漏）；如需瘦身可删 `.venv`（随时可重建） |
| 密钥 | `.env.example` 含 `sk-your-key-here` 占位符（**无真实 .env**）；`test_validator.py` 的 `sk-abcdefghijklmnopqrstuvwxyz1234567890`/`sk-test123` 为测试假数据（该文件本身是泄漏检测器测试用例） | ✅ **无真实密钥** |
| 代码泄漏 | src/tests 中未发现真实 API key 模式 | ✅ |

---

## 三、发现清单

| # | 级别 | 发现 | 建议 |
|---|------|------|------|
| F-1 | P3（残留） | `.workbuddy/outputs/` 8 个 08-03 旧文件 | 删除（有 git 历史，无内容损失）；或归档到 `outputs/_archive/` |
| F-2 | P3（残留） | `.workbuddy/tmp/` 12 个测试残留 | 删除 |
| F-3 | P3（磁盘） | `storm-deep-research/.venv` 66M | 可选删除（可重建）；不动也可 |
| F-4 | P2（文档） | v9.0 宪章八步流程"恢复"步骤无触发定义 | 建议 v9.0 补充"恢复"触发条件（验证失败/文件损坏/沙箱异常 → 回退最近 commit） |

**无 P0/P1。** 无安全泄漏、无副本漂移、无版本不一致。

---

## 四、待 sun 决策

| # | 动作 | 风险 |
|---|------|------|
| D-A | 删除 `.workbuddy/outputs/` + `.workbuddy/tmp/` 残留（20 文件） | 低——均为 untracked 旧产物，正式版在 git 中 |
| D-B | `storm-deep-research/.venv` 是否删除 | 无——可随时重建 |
| D-C | v9.0 是否补充"恢复"步骤触发说明 | 无——纯文档 |

---

*审查纪律：本轮取证仅只读（ls/du/md5/diff/grep），未修改任何文件；清理动作待 sun 批准后执行。*
