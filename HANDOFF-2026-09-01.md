# 2026-09-01 任务交接文档 — Round 8 收尾

> **日期**：2026-09-01
> **仓库**：https://github.com/sunhe-8922/real-estate-appraisal-toolchain
> **规范**：GB/T 50291-2015《房地产估价规范》
> **本轮主题**：数值自洽协议从「人工审查发现」升级为「CI 门禁拦截」
> **下游读者**：下一个接手本项目的 AI 助手或开发者（无需对话上下文）

---

## 一、已完成（Round 8）

### 1.1 核心问题

Round 7 收尾 348 测试全绿，但 **P1-1**（链末节点 `ROUND(area×unit)` 与权威总价差 25 元）是**人工审查翻出来的，不是 CI 拦下来的**。根因：既有测试只证明「双端实现一致」+「schema 合法」，**从不验证「数值自洽」**。一致率 100% ≠ 数值正确。

### 1.2 改动（commit `b042dc8`，tag `round8-done`，**未推送**）

| 文件 | 动作 | 说明 |
|---|---|---|
| `scripts/verify_example_arithmetic.py` | 改 | 泛化（N 实例 / 价值时点可导 / 骨架期 SKIP / 百分比绝对值比对）+ 新增 `calculationChain` 公式↔target 断言 + `--json-out` 机器可读输出 |
| `tests/mutation_harness.py` | 新增 | 变异测试器（27 变异体），含**基线自检**（CONTAMINATED 排除），量化门禁灵敏度 |
| `tests/test_example_arithmetic.py` | 新增 | 常驻门禁：自动发现示例、已知缺陷登记制（unregistered→fail；fixed→fail，比 xfail 严）、覆盖面自检 |
| `tests/test_rounds_evidence.py` | 改 | 存档契约扩展为按类型（diff / mutation），新存档类型不能塞进 diff 字段集 |
| `rounds/8/PROPOSAL.md` `RESULTS.md` + 4 份变异存档 | 新增 | 提案/结果/可复现证据 |
| `rounds/README.md` `.gitignore` | 改 | 索引 + `tests/_tmp_mutants/` 忽略 |

### 1.3 度量（与基线同协议）

| 指标 | 基线 | Round 8 | 判定 |
|---|---|---|---|
| 变异检出率（干净示例） | 93.3% (14/15) | **100% (15/15)** | 提升（+6.7pct，存活 1→0） |
| 校验器覆盖率 | 1/3 | **3/3** | 提升（真实工程 JSON 不再崩溃） |
| 门禁常驻 | 无 | **有** | 提升（改数字即变红，已负向验证） |
| 全量回归 | 348 | **354**（+6） | 不破坏 |

**关键证据**：`mutation_result_round8_baseline.json` 与 `_b1.json` **sha256 完全相同**
（`33660177c6c4e3b3`）——证明 B1「仅泛化、未改断言」是字节级事实，非口头声称。
B2 与 final 同为 `b9406971c73706f6`，证明固化无行为漂移。

### 1.4 本轮挖出的历史缺陷（转 Round 9，需估价专业判断，**不在本轮变量内**）

- **P1-2A**：`schema/example-武汉洪山住宅.json` 的 `income.value` chain 节点公式引用了数据中不存在的键 `growthRate`；公式写的是报酬资本化法，数据实为直接资本化法。
- **P1-2B**：同上文件末节点 `ROUND(area×unit)=3271738` ≠ 权威总价 `3271720`（差 18 元）——与 R7 P1-1 **完全同形态**，该示例从未随整改同步。
- **附带**：`scripts/rebuild_excel_formula.py` 的 `NODE_TOLERANCE=65` 把 18 元偏差放过——容差被调大到恰好容纳错误。该常量需溯源，每个容差应有缺陷编号背书。

---

## 二、未解决 / 风险

1. **Round 9 必须修 P1-2A/P1-2B**：否则住宅示例变异判定持续 CONTAMINATED，变异总数停在 15 而非 27。修复后该示例解锁，覆盖回到全量。
2. **`NODE_TOLERANCE=65` 是隐患**：可能掩盖其他示例的同形态错误。Round 9 审计。
3. **门禁只在 CI 层**：改数据后要到跑测试才红。可考虑编辑时（写入/保存路径）前移拦截（Round 9 P2）。

---

## 三、迭代建议

1. **Round 9 提案候选**（按优先级）：P0 修 P1-2A/P1-2B → 解锁住宅变异覆盖；P1 审计 NODE_TOLERANCE；P2 写入即校验前移。
2. **复用纪律**：B2 直接复用 `rebuild_excel_formula.rebuild_values` 的 ast 求值器，未重复造轮子——后续公式类校验继续走这里。
3. **度量纪律**：任何「提升检出率」的改造，必须先用变异测试器量出基线；新断言上线前先确认基线干净（CONTAMINATED 机制）。

---

## 四、复现命令

```bash
# 全量回归（Python venv：C:/Users/Administrator/.workbuddy/binaries/python/envs/default）
python -m pytest tests/ -q

# 数值自洽门禁（单文件）
python scripts/verify_example_arithmetic.py schema/example-多方法商业.json

# 变异测试（灵敏度量化）
python tests/mutation_harness.py --verbose --json rounds/9/mutation_result_round9_baseline.json

# 基线快照
git tag round9-baseline
```

> 注：`node --test tests/` 在本环境会把目录当模块解析报错；正确用法是按文件或 glob：
> `node --test tests/test_dp_core.js tests/test_e2e_orchestrator.js`（32 passed）。
> `tests/chain_runner.js` / `dp_chain_runner.js` 等是 CLI 工具非测试文件，勿用 `--test` 直接跑。

---

*以上为专业辅助参考，估价结论须由注册房地产估价师审核签署后使用。*
